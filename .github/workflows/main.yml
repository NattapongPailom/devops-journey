name: Docker Build Test
on: [push] # สั่งให้ทำงานทุกครั้งที่มีการ Push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run a one-line script
        run: echo "Hello, GitHub Actions is working!"
